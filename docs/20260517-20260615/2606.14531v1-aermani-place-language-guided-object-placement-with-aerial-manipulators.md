---
title: "AERMANI-PLACE: Language Guided Object Placement with Aerial Manipulators"
title_zh: AERMANI-PLACE：语言引导的空中机械臂物体放置
authors: "Sarthak Mishra, Ritama Sanyal, Rishabh Dev Yadav, Wei Pan, Spandan Roy"
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14531v1"
tags: ["query:aerial-arm"]
score: 10.0
evidence: 语言引导的空中机械臂物体放置
tldr: "空中操纵中的物体放置常需显式的度量坐标，交互不直观。受人类用语言和手势沟通的启发，AERMANI-PLACE框架通过图像编辑模型根据语言指令生成放置标记，再结合深度观测将其转化为度量位置并由空中机械臂执行。在100个指令测试中平均成功率达87%，真实世界转移成功率达72%，展示了语言引导空中放置的有效性和实用性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.14531v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1838, \"height\": 861, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14531v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1838, \"height\": 393, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14531v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 913, \"height\": 815, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14531v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 652, \"height\": 400, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14531v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 664, \"height\": 377, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.14531v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 937, \"height\": 336, \"label\": \"Table\"}]"
motivation: 现有空中机械臂放置需用户提供度量坐标，交互不自然，受人类语言与手势沟通启发。
method: 利用图像编辑模型根据自然语言指令在场景图中生成放置标记，再借助深度信息将标记转化为三维度量点，规划执行轨迹。
result: "在100个语言引导放置任务中测试集成功率达87%，真实平台迁移后达72%，验证了方法有效。"
conclusion: AERMANI-PLACE实现了非度量语言指令到空中放置的映射，提升了人机交互直觉性，为空中操作提供新范式。
---

## 摘要
物体放置是空中操作任务的基本组成部分，然而现有系统通常要求以度量坐标明确指定期望的放置位置。这类界面不够直观，用户需要理解坐标框架和场景几何，使得在实际部署中难以使用。相比之下，人类经常通过语言和指向手势的组合来传达空间目标。受此启发，我们提出了AERMANI-PLACE，一个面向空中机械臂的语言引导物体放置框架。给定场景图像和自然语言指令，图像编辑模型生成修改后的场景版本，其中包含指示物体应放置位置的视觉标记。随后利用深度观测将该标记锚定到物理环境中，恢复度量放置点，然后生成放置轨迹并由空中机械臂执行。我们在100个语言引导放置任务的测试集上评估了所提出的方法，并在真实的空中操作平台上成功执行。实验结果表明，所提方法能够从语言指令中可靠地推断放置位置，在测试集上的平均成功率为87%，并有效迁移到真实世界的空中操作中，平均成功率为72%。视频：https://youtu.be/SgwwgLBsv0g

## 速览
**TLDR**：现有空中物体放置系统需用户以度量坐标指定位置，不直观且需理解坐标系，而人类常以语言和手势沟通空间目标。受此启发，本文提出AERMANI-PLACE框架：输入场景图像和语言指令，图像编辑模型生成带视觉标记的修改图；再通过深度观测将标记定位至物理空间，获取放置点并规划轨迹。在100项测试中，该框架平均成功率达87%，于真实空中机械臂平台也达72%，验证了从语言推断放置位置的可靠性。这一工作实现了语言引导的空中操控，提升了交互直观性，拓展了空中机器人的应用场景。 \
**Motivation**：现有空中物体放置依赖显式度量坐标，不直观且难以使用；人类则习惯语言与手势沟通，这种差距制约了实际应用。 \
**Method**：提出AERMANI-PLACE框架：输入场景图像和语言指令，图像编辑模型生成视觉标记，再通过深度观测恢复3D放置点并规划轨迹。 \
**Result**：在100项测试中达87%平均成功率，真实空中平台达72%，验证了从语言推断放置的可靠性。 \
**Conclusion**：AERMANI-PLACE实现了语言引导的空中物体放置，提升了交互直观性，为空中操控人机交互提供了新范式。

---

## Abstract
Object placement is a fundamental component of aerial manipulation tasks, yet existing systems typically require the desired placement position to be specified explicitly in metric coordinates. Such interfaces are not intuitive and require users to reason about coordinate frames and scene geometry, making them difficult to use in practical deployments. In contrast, humans often communicate spatial goals through a combination of language and pointing gestures. Inspired by this observation, we present AERMANI-PLACE, a framework for language-guided object placement with aerial manipulators. Given a scene image and a natural language instruction, an image editing model generates a modified version of the scene containing a visual marker that indicates where the object should be placed. This marker is then grounded into the physical environment using depth observations to recover a metric place point, after which a placement trajectory is generated and executed by the aerial manipulator. We evaluate the proposed approach on a test set of 100 language-guided placement tasks and demonstrate successful execution on a real aerial manipulation platform. Experimental results show that the proposed method reliably infers placement locations from language instructions with an average success rate of 87\% on the test-set and transfers effectively to real-world aerial manipulation with an average success rate of 72\%.   Video: https://youtu.be/SgwwgLBsv0g

---

## 论文详细总结（自动生成）

好的，以下是对论文《AERMANI-PLACE: Language Guided Object Placement with Aerial Manipulators》的详细中文总结。

### 1. 论文的核心问题与整体含义

*   **核心问题**：现有空中机械臂（Aerial Manipulator, AM）在执行物体放置任务时，需要用户以不直观、易出错的显式度量坐标（如“将物体放置于（x, y, z）”来指定目标位置。这种交互方式不符合人类习惯于用语言和指向手势沟通空间意图的自然范式。
*   **研究动机**：受人类沟通方式启发，论文旨在构建一个更自然、无需训练的框架，使空中机械臂能够直接理解自然语言指令（如“把电池放进红色箱子”），并自主推断出物理世界中的可操作放置点。
*   **整体含义**：将物体放置任务重新定义为视觉“指向”问题，利用生成式模型的语义理解能力架起语言指令与物理世界之间的桥梁，从而提升空中操作系统的自主性和人机交互的直觉性。

### 2. 论文提出的方法论

核心思想是利用现成的图像编辑模型作为零样本空间推理器，将语言指令转化为图像中的视觉标记，再通过几何流程将其锚定到三维世界。

*   **四阶段流水线**：
    1.  **语言引导的目标定位（Language-Guided Goal Localization）**：
        *   给定场景RGB图像 `I` 和语言指令 `L`，使用图像编辑模型 `Φ`（如Nano Banana Pro）生成修改后的图像 `I'`。
        *   `I'` 中在目标放置位置会生成一个高对比度的视觉标记（如绿色圆点），其像素坐标为 `u`。此过程通过精心设计的结构化提示词来保持场景一致性，防止模型产生幻觉。
    2.  **三维放置恢复（3D Placement Recovery）**：
        *   **点锚定**：利用配准的深度图 `D` 获取标记点处的深度值 `D(u)`，然后结合相机内参 `K` 和相机位姿 `T_cam`，通过反投影将像素坐标 `u` 恢复为世界坐标系中的初始度量放置点 `P_init`。
            `P_init = T_cam · [ (u-cx)*D(u)/fx, (v-cy)*D(u)/fy, D(u), 1 ]^T`
        *   **物体几何重建**：使用物体在抓取前的RGB-D观测，通过分割掩码生成物体点云 `C_O`，并提取其最低的支撑点集 `C_base`。
        *   **表面对齐**：将 `C_base` 平移到 `P_init`，确保物体底部与预测的放置点对齐，得到初始三维放置配置。
    3.  **碰撞感知的调整与执行（Collision-Aware Adjustment and Execution）**：
        *   **局部优化函数 `f`**：对 `P_init` 进行精细化调整以解决碰撞问题。先优化z轴（高度），通过“触地”方法将物体上升至无碰撞，再下放至首次接触表面；若存在横向碰撞，则在xy平面局部搜索最近的碰撞自由点 `P*`。
        *   **运动规划与执行**：生成一条自上而下的轨迹。AM先从当前位置 `X_curr` 移动到 `P*` 上方固定偏移量 `h_offset` 处的接近点，然后垂直下降至释放高度 `z_release = z_P* + δ`（δ为安全余量），释放物体后垂直上升撤离。

### 3. 实验设计

*   **数据集/场景**：
    *   **测试集**：在实验室环境中构建了包含100个多样化放置任务的测试集。每个任务包含：物体抓取前和放置场景的RGB-D图像对、一条语言指令 `L`、以及人工标注的三维真值坐标 `P_GT`。任务覆盖四个难度类别：直接表面、相对定位、语义消歧和可变纹理表面。
    *   **真实硬件平台**：在自研的空中机械臂平台上进行了25次真实世界试验，覆盖相同的四种难度类别。
*   **对比基线**：
    *   **真值（GT）**：使用人工标注的 `P_GT`，代表理论上限。
    *   **用户点击（User-Click）**：人类操作员在图像上手动点击目标点，作为有人参与的基准。
    *   **三种前沿方法**：RoboPoint（基于VLA的关键点预测）、AnyPlace（通用物体放置框架）、Moka（基于标记的视觉提示）。实验确保所有基线接收相同信息。

### 4. 资源与算力

*   **文中未明确说明模型的训练细节或所需的GPU型号/数量**，因为方法本身是“免训练”的，采用现成的图像编辑模型（如Nano Banana Pro, Qwen-VL-Edit, Kling 1.5 Edit）。
*   论文提到了基线方法是在本地配备高端GPU的计算机上实现的，但未提供具体型号。空中机械臂平台使用的机载计算设备为NVIDIA Jetson Nano Super。

### 5. 实验数量与充分性

*   **实验组数**：主要包括两大类实验：
    1.  **大规模测试集评估**：在100个任务上进行了定量评估。
    2.  **真实硬件验证**：在物理平台上进行了25次端到端试验。
*   **充分性与客观性**：
    *   **充分性**：测试集规模较大，涵盖多难度类别，能较全面地评估语言理解、定位精度和任务成功率。硬件实验验证了方法的现实可行性。实验还对比了不同的图像编辑模型（3种），并对失败案例进行了分类分析。
    *   **客观性与公平性**：通过使用相同输入和统一条件来对比不同基线，保证了公平性。评估指标（标记预测成功率、放置误差、放置成功率）定义清晰。

### 6. 论文的主要结论与发现

*   AERMANI-PLACE框架能够可靠地从语言指令中推断放置位置，在100个任务测试集上达到87%的成功率，且为免训练方法，性能可与经过训练的SOTA模型（如RoboPoint 82%）相媲美。
*   在真实空中机械臂平台上，系统成功执行大部分任务（72%成功率），证明了方法从仿真到真实环境的有效迁移。
*   “自上而下”的放置策略和碰撞感知的几何细化对于减轻空中系统的传感器噪声和气动干扰至关重要。
*   虽然生成式模型提供了通用的免训练接口，但其概率性质导致的幻觉、场景失真等是主要失败原因，需要鲁棒的下游几何校验。

### 7. 优点

*   **免训练**: 直接利用现成的图像编辑模型，无需针对空中操作任务收集大量数据和训练，降低了应用门槛。
*   **直观交互**: 将语言指令作为输入，符合人类操作习惯，消除了对不直观度量坐标的需求。
*   **方法简洁有效**: 将复杂的语言引导放置问题巧妙转化为视觉“指向”任务，并通过几何处理使其可执行。
*   **真实系统验证**: 方法在实物无人机平台上进行了完整的端到端验证，而不仅是仿真，增强了对实际应用潜力的说服力。

### 8. 不足与局限

*   **方向姿态缺失**: 当前工作仅预测三维位置点，无法推理6自由度的物体放置方向，限制了其在精确装配任务中的应用。
*   **生成式模型的不确定性**: 框架依赖的图像编辑模型可能产生物体幻觉、场景失真或标记错位，直接导致任务失败，且方法对这种高层错误鲁棒性有限。
*   **单一视角局限**: 论文提到从单一视角预测完整姿态很困难，依赖深度传感器，而深度噪声（尤其是边缘和反射表面）可能导致锚定失败。
*   **物理交互复杂场景未覆盖**: 实验在相对静态的室内场景进行，未涉及动态环境或需要复杂物理推理（如堆放、插入）的任务。
*   **硬件平台约束**: 实验使用Optitrack动作捕捉系统进行高精度状态估计，限制了系统在未知环境中的部署。

（完）
