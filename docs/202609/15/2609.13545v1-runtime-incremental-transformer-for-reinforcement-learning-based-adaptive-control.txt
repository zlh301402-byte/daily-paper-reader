Title: Runtime-Incremental Transformer for Reinforcement-Learning-Based Adaptive Control

URL Source: https://arxiv.org/pdf/2609.13545v1

Published Time: Tue, 15 Sep 2026 00:19:32 GMT

Number of Pages: 16

Markdown Content:
# Runtime-Incremental Transformer for Reinforcement-Learning-Based Adaptive Control 

# Giansalvo Cirrincione a, Adriano Fagiolini b,∗

> a

Laboratoire LTI, Universit´ e de Picardie Jules Verne, Amiens, France 

> b

MIRPALab, Department of Engineering, University of Palermo, 90128 Palermo, Italy 

Abstract 

Learning-based adaptive control of robotic manipulators with non-observable friction memory has been addressed by attention-based meta-controllers whose number of attention heads is fixed before training and is tuned by costly offline search. At long memory horizons, such fixed-capacity controllers are prone to catastrophic failures on a sizeable fraction of training seeds. The present paper introduces a runtime mechanism that grows and prunes the heads of the attention block during reinforcement learning, governed by two signals: the effective rank of the on-policy context distribution, which triggers growth when representational capacity becomes insufficient, and the per-head output magnitude, which flags redundant heads for removal. Policy continuity at growth events and a quantitative bound at prune events are established analytically. On a two-link manipulator with Stribeck friction, the proposed mechanism attains full success across all memory regimes, eliminating the long-horizon failure mode and removing the need for offline tuning of the head count. 

Key words: Adaptive control, reinforcement learning, transformer architectures, curriculum learning, Stribeck friction, Euler–Lagrange systems 

1 Introduction 

Robot manipulators are commonly controlled by computed-torque laws whose gains are adapted online to compensate for uncertainties in inertia and friction. The passivity-based construction of Slotine and Li [23] handles the case in which the uncertainty is linear in an unknown parameter vector; viscous and Coulomb fric-tion fall into this case. Stribeck and LuGre friction [2,4] do not: the friction force now depends on a hidden internal state that decays over a finite time, denoted 

τz and called the memory horizon of the plant. Since the hidden state cannot be recovered from the instan-taneous joint coordinates, the closed-loop system is no longer Markovian in the measured state, and classical adaptive arguments no longer apply directly. A companion paper by the same authors [11] addresses the non-Markovian regime by feeding a sliding window of past measurements into a reinforcement-learning (RL) 

∗ Corresponding author. Tel. +39 091 238 63613. This paper was not presented at any IFAC meeting. 

Email addresses: exin@u-picardie.fr (Giansalvo Cirrincione), fagiolini@unipa.it (Adriano Fagiolini). 

policy that outputs the gains of the computed-torque law. The policy is a single-layer transformer encoder; fol-lowing the naming in the companion paper, it is denoted INCRT-1L, a single-layer instance of the Incremental Transformer (INCRT) family. The number of attention heads, denoted K, is the architectural parameter that controls the representational capacity of the encoder. INCRT-1L is trained once per task, with K selected before training by an incremental rank-tracking proce-dure [9] applied to a surrogate operator. The trained controller closes roughly 35% to 40% of the tracking er-ror left by the computed-torque baseline at short and medium memory horizons, τz ∈ { 1, 2} s. At the longer horizon τz = 5 s, the same procedure fails on four of ten random seeds; the trained policies either diverge or col-lapse onto a payload-invariant response whose tracking error exceeds that of the uncompensated baseline. The failure is attributed in [11, Sec. 7.3] to local attractors of the RL optimisation that are invisible to the offline surrogate analysis. The present paper carries out a programme explicitly announced in the abstract of [11]: the rank-tracking dy-namics are moved inside the RL loop, and the attention heads are added and removed during training rather than fixed in advance. The resulting architecture is referred to 

Preprint submitted to Automatica 15 September 2026 arXiv:2609.13545v1 [cs.RO] 11 Sep 2026 as Runtime-INCRT . Runtime head addition is governed by a cooldown parameter ∆ grace (the minimum num-ber of training steps between two events) and a thresh-old εgrow on the effective rank of the recent context-token distribution; runtime head removal is governed by a threshold εprune on the per-head output magnitude. The resulting mechanism is simpler in operation than the Phase-1 / Phase-2 procedure it replaces, yet yields better tracking in all three memory regimes and removes the need for per-task hyperparameter search. Three architectural guarantees are established for the runtime mechanism (Section 5). Every grow event is con-tinuous at the policy level, since the newly activated head is initialised to contribute zero output (Proposition 7). Every prune event changes the policy by at most a pre-scribed multiple of εprune , and the cumulative change along the training horizon is bounded in terms of the number of prune events (Theorem 9). The active head count, denoted K(t), converges to a steady-state interval whose width is set by εgrow and εprune , provided the ef-fective rank of the context-token distribution has a limit (Proposition 11). Corollary 13 assembles the three re-sults into the no-regression property required by Prob-lem 4. Empirically, Runtime-INCRT is evaluated on the two-degree-of-freedom (2-DOF) Stribeck benchmark of the companion paper, with ten random seeds per mem-ory regime τz ∈ {1, 2, 5} s (Section 7). A single set of runtime-controller hyperparameters, shared across the three regimes, gives 30 /30 successful runs and, at 

τz = 5 s, eliminates the failure mode reported in [11]. The controller does not, however, adapt the active head count to task complexity as one might expect: it sat-urates the head-count cap Kmax in every run of the main campaign, the growth schedule is identical across memory regimes, and the prune trigger never fires. Two ablations clarify what is in fact happening. Sweeping 

Kmax over {2, 4, 8, 16 } at τz = 2 s shows that the smallest cap gives the best mean and the lowest per-seed variance, and Kmax = 16 is worse than Kmax =2 — larger capacity does not help. Sweeping ∆ grace 

over {1 000 , 2 000 , 5 000 } shows that performance is non-monotone in the cooldown, with the default value opti-mal and both extremes producing catastrophic seed fail-ures. These observations together suggest that Runtime-INCRT’s benefit comes from the training trajectory, not from the terminal capacity: the gradual activation of heads acts as a capacity curriculum internal to a single RL training run. Section 2 reviews related work. Sections 3–4 fix the problem formulation and the variable-head attention architecture. Section 5 defines the runtime controller and proves the three guarantees. Sections 6–7 describe the experimental protocol and report results. Section 8 draws the conclusions. Proofs are in Appendix A; hyper-parameter values in Appendix B. 

2 Related work Adaptive control and memory-aware policies. 

The passivity-based adaptive controller of Slotine and Li [23] and its computed-torque refinements [24] deliver asymptotic tracking when friction is linear in the un-known parameters. For Stribeck and LuGre models [2,4], the friction force is no longer in the scope of a known regressor, and classical adaptive arguments no longer apply. A Lyapunov-shielded residual reinforcement-learning framework for this regime [10] adds a torque correction to the nominal law under closed-form ad-missibility projection; the parent paper of the present work [11] lifts this to parameter-level control, with an online Soft Actor-Critic (SAC) algorithm [15] acting on a windowed history of motion. Transformer-based RL policies are otherwise mostly confined to the offline setting [6,18]; Long Short-Term Memory [17] and Gated Recurrent Unit [8] recurrences are the classical alterna-tives. The architecture presented in this paper retains the online-SAC backbone and modifies only the feature extractor. 

Dynamic-capacity architectures and curricula. 

Growing networks have a long history [12], with modern instances in Net2Net [7] and progressive training [19]; pruning is the dual operation [16,21]. Differentiable neu-ral architecture search [20] and its RL counterparts [22] perform the search on a surrogate and retrain the result-ing fixed architecture on the target task; the Phase-1 / Phase-2 separation of INCRT [9] and INCRT-1L [11] fol-low this pattern. Transformer-specific growth has been explored for training acceleration [13,5]. Curriculum learning in its task-scheduling form [3,14,1] is distinct from capacity curricula: the present paper’s finding is that gradual capacity growth, applied inside a single on-line RL training run and driven endogenously by the rank of the on-policy context distribution, acts as an im-plicit capacity curriculum. Three distinctions relative to the above literature are specific to the present setting: (i) the capacity change takes place during a single online RL run, not in a search-and-retrain loop; (ii) each change is policy-preserving by construction (Proposition 7), which enables uninterrupted SAC training across the event; and (iii) the driver of growth is the effective rank of the context-token distribution induced by the current pol-icy, not of a pre-specified surrogate. 

3 Problem setup 

This section fixes the class of systems addressed by the paper and introduces the notation used in Sections 4–7. The formulation is inherited from the parent work [11], with the necessary definitions reproduced here so that the paper is self-contained. 23.1 Euler–Lagrange systems with unobservable mem-ory 

Consider rigid-body manipulators with n degrees of free-dom, described by the Euler–Lagrange equation 

M (q) ¨ q + C(q, ˙q) ˙ q + G(q) + F (q, ˙q, z ) = τ, (1) where q ∈ Rn are the generalised coordinates, M (q) ≻ 0is the mass matrix, C(q, ˙q) ˙ q collects Coriolis and cen-tripetal terms, G(q) is the gravity vector, and τ ∈ Rn

is the joint-torque input. The vector F (q, ˙q, z ) accounts for non-conservative effects — here, friction — and de-pends on an unobservable memory state z ∈ Rnz whose evolution is governed by ˙z = ζ(q, ˙q, z ). (2) The state z is not instantaneously recoverable from ( q, ˙q)and the flow of (2) is contractive with a finite time-constant Hz = ∥∂ζ/∂z ∥−1, the memory horizon of the plant. Assumption 1 collects the regularity conditions on the dynamics inherited from [11]. 

Running example: Stribeck friction. Throughout the paper, F is instantiated as the Stribeck friction model 

Fs( ˙ q, z ) = Fc +  F max  

> s

− Fc

 exp  −( ˙ q/v s)2×

sign( ˙ q) + σ ˙q + z, (3) with memory dynamics ˙z = − z/τ z + λz ˙q, (4) so that Hz = τz . The parameter τz is the principal sweep axis of the experimental evaluation of Section 7 and takes values in {1, 2, 5} s. The Stribeck construction is a canonical surrogate for a broader class of unobservable-memory phenomena in manipulation, including joint elasticity, soft-contact hysteresis, and damper dynam-ics [2,4]. 

3.2 Tracking task and extended state 

Given a smooth reference trajectory qd(t), the control task is to track qd under unknown memory z(t) and an unknown static payload parameter p ∈ [pmin , p max ] that modifies the effective mass in M . The tracking error is 

e = qd − q with velocity error ˙ e = ˙ qd − ˙q. Following the conventions of [11], the extended state used throughout the paper is defined as 

x = ( q, ˙q, e, ˙e, s ) ∈ R5n, (5) where s collects auxiliary variables (sliding surface, fil-tered references). The cost functional is 

J (π) = Ep, z (·), q d(·)

"Z T

> 0

ℓ e(t), ˙e(t) dt 

#

, (6) with ℓ strongly convex in its arguments. The expecta-tion is taken over the task distribution (random payload, reference, and initial condition) and over the realisation of the memory state z(t) conditional on the motion. 

3.3 Controller template and admissibility 

The torque applied to the manipulator follows the pa-rameterised computed-torque structure of [11] 

τ (t) = CT  q, ˙q, q d; Kd(t), Λ( t) + ϕff 

 q, ˙q; η(t), (7) where CT is the computed-torque law with gain ma-trices Kd, Λ, and ϕff (q, ˙q; η) = Φ( q, ˙q) η is affine in the feed-forward weights η. The parameter trajectory 

θctrl (t) = ( Kd(t), Λ( t), η (t)) lives in a compact box P =[Kmin  

> d

, K max  

> d

] × [Λ min , Λmax ] × [ηmin , η max ]dim η . Affinity of ϕff in η is retained to preserve the convexity of the admissibility set of [11, Lemma 4]; the state-dependent feature matrix Φ may be the output of an arbitrary deep network without compromising this convexity. 

3.4 Standing assumptions 

The theoretical results of Section 5 and the experimen-tal protocol of Section 7 rely on the following standing assumptions, inherited from [11] and standard in the computed-torque literature [23,24]. 

Assumption 1 (Regularity of the dynamics) 

M, C, G are smooth on the operating set; F is contin-uous in (q, ˙q, z ) and Lipschitz in z; ζ is Lipschitz and generates a contractive flow with rate 1/H z .

Assumption 2 (Reference regularity) qd ∈

C2([0 , T ]) with uniformly bounded derivatives. 

Assumption 3 (Parameter compactness) The pa-rameter set P is a compact box; the squashing functions of Section 4.3 land in its interior by construction. 3.5 The meta-controller family 

The meta-controller is a map 

Gθ : RW ×dc −→ P , (8) parameterised by θ ∈ Rdθ , that consumes a context token 

c(tk) =   o(tk−W +1 ), o (tk−W +2 ), . . . , o (tk) 

∈ RW ×dc , (9) 3and produces the gain tuple θctrl (t) = Gθ (c(t)) to be substituted into (7). Each step-observation o(t) concate-nates ( q, ˙q, q d, ˙qd, ˆp, ˆμ, t/T ) ∈ Rdc , with ˆ p a noisy pay-load estimate, ˆ μ a friction-regime estimate held at 0 .2, and t/T a phase indicator. For the experiments of Sec-tion 7 the degree of freedom is n = 2, so dc = 4 n+3 = 11. 

Window selection. The window size W is the unique hyperparameter of the context token not fixed by the dynamics. The lower bound of Proposition 1(iii) of [11] requires W ≥ Hz /∆t to close the Markovian optimality gap, which for Stribeck gives W ≥ τz /∆t: at ∆ t = 10 ms, this reads W ≥ 100 for τz = 1 s, W ≥ 200 for τz = 2 s, and W ≥ 500 for τz = 5 s. In the main experiments W =20 is adopted at τz ∈ { 1, 5} and W = 50 at τz = 2; both values are below the Markov-optimality bound at the corresponding horizon, which is a deliberate stress test of the windowed architecture and matches the setting of [11]. 

3.6 Problem statement 

The meta-controller family is indexed by the architec-tural parameter K, the number of attention heads con-sumed by Gθ to process the history window. The parent work [11] selects K offline, by applying an incremental rank-tracking analysis [9] to a surrogate temporal resid-ual operator, before training the policy via reinforce-ment learning at fixed K = K⋆. This Phase-1 / Phase-2 separation , while principled and empirically effective at τz ∈ { 1, 2} s, admits a documented failure mode at the long-memory regime τz = 5 s, where 4 /10 training seeds diverge or collapse to a payload-invariant policy ([11, Section 7.3]). The failure is attributed in the parent work to “local attractors of the reinforcement-learning optimisation” that are not detectable from the surrogate analysis. This paper addresses the following problem. 

Problem 4 (Runtime head selection) Design aprocedure that selects the active head count K(t) during 

reinforcement-learning training, such that (P1) the per-task offline architecture search of Phase 1 is obviated; (P2) the resulting policy at training termination matches or exceeds, in tracking performance, the best fixed-K

policy selected by the Phase-1 / Phase-2 procedure, across the memory horizons τz ∈ { 1, 2, 5} s; (P3) the transformation of K(t) during training satisfies a no-regression property: the policy output is contin-uous at every head-count change, and the parameter energy is bounded along the event sequence. 

Requirement (P1) is a simplification of the engineering pipeline; requirement (P2) is the empirical criterion for success; requirement (P3) is the architectural guarantee that distinguishes a principled runtime adaptation from a naive restart-and-resize. 

4 Variable-head attention architecture 

The meta-controller architecture used to address Prob-lem 4 is specified next. The key design is a variable-head attention block whose active head count K can be modi-fied during training via two primitive operations — grow 

and prune — without interrupting the reinforcement-learning loop. Section 5 gives the controller that sched-ules these operations and establishes the guarantees de-manded by (P3). 

4.1 Variable-head attention block 

Let Kmax denote the head-count cap fixed at de-sign time, and dk the per-head dimension; the model dimension is dmodel = Kmax · dk. The block consists of Kmax heads, each parame-terised by four matrices (W Qi , W Ki , W Vi , W Oi ) ∈

Rdmodel ×dk × Rdmodel ×dk × Rdmodel ×dk × Rdk ×dmodel , and an active mask m ∈ { 0, 1}Kmax . The forward pass on a sequence h ∈ RW ×dmodel is VarAttn( h) = h + X  

> i:mi=1

head i(h), (10) where the causal per-head map is head i(h) = softmax 

 hW Qi (hW Ki )⊤

√dk

+ M



hW Vi W Oi ,

(11) and M is the upper-triangular causal mask. The resid-ual connection in (10) is a deliberate choice: it ensures that a head whose output is zero contributes zero to the forward pass, a property exploited by the grow operation below. The meta-controller Gθ is realised by stacking the variable-head attention block on the input-embedded context token, taking the output of the last time-step as the feature vector, and passing it through a fixed MLP head Rdmodel → P (Section 4.3). The active head count at step t is 

K(t) = PKmax  

> i=1

mi(t). (12) The two primitive operations that modify K(t) are de-scribed next. 

4.2 Grow and prune primitives 

Grow. The Grow( i) operation activates a previously-inactive head i (i.e., mi = 0 → mi = 1) and reinitialises 4its parameters according to the following scheme, de-noted soft initialisation :

W Qi , W Ki , W Vi ← Xavier normal , (13) 

W Oi ← 0. (14) The rationale for (14) is that head i(h) = ( · · · )W Oi , so setting W Oi = 0 makes head i contribute zero to the sum in (10) at the instant of growth, regardless of the query/key/value initialisation. The resulting policy out-put is therefore unchanged at the growth event, a prop-erty stated formally and numerically verified in Propo-sition 7. After growth, all four matrices of head i are registered as trainable in the reinforcement-learning op-timiser. 

Prune. The Prune( i) operation deactivates an active head i (mi = 1 → mi = 0). The parameters of head i

are frozen ( requires grad = False ) and removed from the optimiser state, so that no further gradient update affects them. The removed head is excluded from the sum in (10), which induces a one-step discontinuity in the policy output of magnitude at most ∥head i(h)∥2 at the pruning instant. This discontinuity is bounded by the prune-trigger threshold by design (Section 5). 

Bookkeeping. Grow and prune events invalidate the optimiser’s internal state (Adam’s first- and second-moment buffers), since the set of trainable parameters changes. The optimiser is rebuilt after every event by re-registering the active parameters and carrying the cur-rent learning rate forward from the scheduler. The re-play buffer of the reinforcement-learning algorithm is not 

cleared: Proposition 7 ensures that the stored transitions remain on-policy at the growth event up to numerical precision; at prune events the transitions are at most 

εprune -off-policy, where εprune is the pruning threshold (Section 5). 

4.3 Output mapping to P

The MLP head of Gθ produces a raw action a ∈ Rdim P ,which is squashed into the compact parameter set P by 

Kd(t) = Kmin  

> d

+ σ(aK ) ⊙ (Kmax  

> d

− Kmin  

> d

), (15) Λ( t) = Λ min + σ(aΛ) ⊙ (Λ max − Λmin ), (16) 

η(t) = ηmax ⊙ tanh( aη ), (17) where σ denotes the sigmoid. The squashing is identi-cal to [11, Section 4.3] and ensures by construction that 

θctrl (t) ∈ P , preserving Assumption 3 throughout train-ing. 

4.4 Training loop 

The meta-controller is trained by SAC on the single-step meta Markov decision process (MDP) of [11, Sec-tion 4.4]. The shielded admissibility constraint of the parent work is retained unchanged: at every SAC up-date, the loss is augmented by a Lagrangian penalty on the distance of Gθ (c(t)) from the admissible set Πadm (x(t)), and a final projection onto Π adm (x(t)) is ap-plied at runtime ([11, Theorem 3(b)]). The new element in this paper is the runtime controller Crt that invokes Grow and Prune on the variable-head attention block at selected training steps, interleaved with the SAC up-dates. The controller is defined in Section 5; its interface is specified here. 

Inputs. At every check step t ∈ { tchk , 2tchk , . . . }, Crt 

reads the current active head count K(t), a rolling buffer of recent encoder-input tokens, and the per-head output norms evaluated on a fresh mini-batch. 

Outputs. One of three actions: noop , Grow( i) for some inactive i, or Prune( i) for some active i̸ = arg max. 

Frequency. Check steps occur at a fixed cadence tchk ;consecutive events are separated by a minimum cooldown ∆ grace ≥ tchk to allow the policy to adapt to the architectural change before the next one is triggered. 

5 Runtime controller and guarantees 

This section specifies the runtime controller Crt that schedules the grow and prune operations of Section 4.2 during reinforcement-learning training, and establishes three results that jointly answer requirement (P3) of Problem 4. Section 5.1 defines the online effective-rank signal on which the grow trigger is based. Section 5.2 gives the deterministic event rules. Section 5.3 states and proves the three guarantees: Proposition 7 (zero-loss-at-growth), Theorem 9 (a policy-continuity bound along the entire event sequence), and Proposition 11 (conver-gence of the active head count to a steady-state interval). Corollary 13 summarises how the three results jointly de-liver (P3). Proofs are given inline; a fully rigorous treat-ment of Proposition 11 is deferred to Appendix A. 

5.1 Online effective-rank signal 

The grow rule is driven by an estimate of the effective rank of the stream of encoder-input tokens, i.e., of the distribution of context tokens c(t) observed during train-ing. The estimate is online and non-anticipative : it uses only tokens collected up to the current training step. 

Definition 5 (Empirical effective rank) Fix abuffer size N and an energy threshold α ∈ (0 , 1) . At train-ing step t, let Bt = [ c(t − N + 1) , . . . , c (t) ] ⊤ ∈ RN ×W d c

be the matrix of the last N context tokens (flattened), 

5Check step tchk     

> Cooldown ≥∆grace ?Measure ˆ ρα(t), πi(t)ˆρα>K (t)(1+ εgrow )? min iπi<ε prune ?Resume SAC update Grow Prune Noop Noop
> yes no no no yes yes

Fig. 1. Decision logic of the runtime controller Crt at each check step. A grow event is triggered when the empirical effective rank ˆ ρα exceeds the current head count scaled by (1 + εgrow ); a prune event is triggered when the relative out-put πi of the weakest active head falls below εprune . At most one event fires per check; consecutive events are separated by a cooldown of ∆ grace steps. 

centred column-wise. Let σ1(t) ≥ σ2(t) ≥ · · · ≥ 0 be the singular values of Bt. The empirical effective rank at threshold α is 

ˆρα(t) := min 

(

k ≥ 1 : 

Pkj=1 σj (t)

P 

> j≥1

σj (t) ≥ α

)

. (18) Throughout the paper, N = 10 3 and α = 0 .95 are used, as in the main experiments of Section 7. The quantity ˆρα(t) takes integer values in [1 , W d c] and is bounded independently of K(t). 

Remark 6 (Relation to the INCRT rank signal) 

Definition 5 is the on-line analogue of the residual rank used in the Phase-1 search of [11, Section 6]. The parent procedure evaluates reff once, on a surrogate temporal residual operator constructed offline, prior to policy training. The present estimate is updated at every check step and does not require the construction of a surrogate operator: it measures directly the rank of the distribution of context tokens induced by the current policy. 5.2 Event rules 

Let tchk be the check cadence and ∆ grace ≥ tchk the cooldown. At each check step t = n t chk , the controller evaluates the grow trigger, then the prune trigger, and applies at most one event per check. Figure 1 summarises the decision flow. 

Grow trigger. Let K(t) denote the current active head count. The controller maintains a counter κgrow (t) in-cremented whenever ˆρα(t) > K (t)  1 + εgrow 

 and K(t) < K max ,

(19) and reset to zero otherwise. When κgrow (t) ≥ δgrow and the cooldown has elapsed (at least ∆ grace training steps since the previous event), the controller selects an in-active head index i and performs Grow( i). The grow counter is reset and the cooldown clock restarted. 

Prune trigger. For each active head i, let ηi(t) := 

∥head i(ht)∥2 be the output norm of head i on a fresh mini-batch of encoder inputs ht, and let 

πi(t) := ηi(t)

P   

> j:mj(t)=1

ηj (t) (20) be its relative contribution to the attention block’s out-put. The controller maintains a counter κ(i)prune (t) incre-mented whenever πi(t) < ε prune and K(t) > K min , and reset to zero otherwise. When κ(i)prune (t) ≥ δprune and the cooldown has elapsed, the controller performs Prune( i)and restarts the cooldown. 

Hyperparameter set. The runtime controller exposes six hyperparameters: 

εgrow , ε prune , δ grow , δ prune , ∆grace , t chk . Their values for the main campaign are summarised in Table B.1 of Appendix B; empirical sensitivity to each is reported in Section 7. 

5.3 Guarantees 

The three properties announced above are established below. The first is purely architectural and holds exactly by construction. 

Proposition 7 (Zero-loss-at-growth) Let t∗ be the training step at which Grow( i) is applied, with the soft initialisation (13) –(14) . Then for every input sequence h

to the variable-head attention block, 

VarAttn m(t∗+) (h) = VarAttn m(t∗−)(h), (21) 

and consequently, on every context token c,

Gθ(t∗+) (c) = Gθ(t∗−)(c). (22) 

Proof. By (10), VarAttn m(t∗+) (h) = h + X   

> j:mj(t∗+)=1

head j (h)= VarAttn m(t∗−)(h) + head i(h),

6since Grow( i) adds exactly one term to the active-head sum. From (11), head i(h) = softmax( · · · ) hW Vi W Oi .The soft initialisation (14) sets W Oi = 0, so head i(h) = 0 and the two attention outputs coincide. Since all down-stream blocks of Gθ (layer-normalisation, MLP head, squashing) are deterministic in their inputs and are not affected by the mask update, Gθ(t∗−)(c) = Gθ(t∗+) (c)for all c. 2

Remark 8 (Numerical verification) In the PyTorch implementation used for the experiments of Section 7, the two sides of (21) were measured to coincide at 

VarAttn m(t∗−)(h) − VarAttn m(t∗+) (h) ∞ = 0 

to 32-bit floating-point precision on every invocation of 

Grow , confirming the absence of numerical drift intro-duced by the mask update or the optimiser rebuild. 

The second guarantee quantifies the jump in the meta-controller output at prune events. Grow events are con-tinuous by Proposition 7; prune events, by design, are not, but their discontinuity is bounded by the prune threshold εprune . The next theorem makes this precise. 

Theorem 9 (Policy-continuity bound) Let MLP : 

Rdmodel → P denote the MLP head of the meta-controller, 

LMLP its Lipschitz constant on the image of the attention block, and σmax an upper bound on the output norm of a single attention head across the operating set. Let t∗ 

> g

and 

t∗ 

> p

denote, respectively, a grow event and a prune event, and let c be any context token. Then: 

Gθ(t∗ 

> g+)

(c) − Gθ(t∗ 

> g−)

(c) = 0 , (23) 

Gθ(t∗

> p+)

(c) − Gθ(t∗

> p−)

(c) ≤ LMLP εprune · S(t∗

> p

),

(24) 

Consequently, along the entire sequence of events pro-duced by the runtime controller, 

X

> t∗
> g

Gθ(t∗ 

> g+)

(c) − Gθ(t∗ 

> g−)

(c)

+ X

> t∗
> p

Gθ(t∗

> p+)

(c) − Gθ(t∗

> p−)

(c)

≤ LMLP εprune Kmax σmax Np(t),

(25) 

where Np(t) is the total number of prune events up to step t, bounded by Np(t) ≤ Kmax − Kmin + Ng(t).

Proof. Equation (23) is Proposition 7. For (24), let i

be the head pruned at t∗

> p

. By construction of the prune trigger (Section 5.2), at the instant of pruning πi(t∗

> p

) <εprune , which together with (20) gives 

ηi(t∗

> p

) < ε prune 

X   

> j:mj(t∗
> p−)=1

ηj (t∗

> p

). (26) Removing head i from the active-head sum in (10) changes the attention-block output by exactly 

−head i(ht), whose norm is ηi(t∗

> p

) by definition. Propa-gating this change through the LMLP -Lipschitz down-stream map and combining with (26) yields (24). The cumulative bound (25) follows by summing the per-event jumps, observing that there are no contributions from grow events (by (23)), and bounding each ηj by 

σmax with the number of active heads by Kmax . The count Np(t) ≤ Kmax − Kmin + Ng(t) is an elementary consequence of the fact that prune events decrement K

(bounded below by Kmin ) and grow events increment it (bounded above by Kmax ). 2

Remark 10 (Interpretation) Theorem 9 states that the runtime controller never introduces a policy discon-tinuity larger than a prescribed multiple of εprune at any single event, and that the cumulative discontinuity along the entire training horizon grows at most linearly in the number of prune events. Grow events contribute nothing to this sum. In the limit εprune → 0, the policy trajectory is fully continuous; for the values of εprune used in Sec-tion 7 ( εprune = 10 −3), the maximum per-event jump in parameter space is approximately 0.1% of the scale of P,which is smaller than the per-step displacement of θctrl 

induced by a single SAC gradient update on the same controller. 

The third guarantee addresses the long-time behaviour of the active head count K(t). The result is stated con-ditionally on an assumption on ˆ ρα that is testable a pos-teriori; Section 7 reports its empirical status. 

Proposition 11 (Convergence of K(t)) Suppose that the empirical effective rank admits a limit along the cooldown-separated subsequence tn = n∆grace :

ˆρ⋆α := lim  

> n→∞

ˆρα(tn) ∈ Z≥1. (27) 

Then the active head count K(tn) produced by the event rules of Section 5.2 converges to a steady-state value 

K⋆ ∈ { Kmin , . . . , K max } satisfying 

 ˆρ⋆α

1 + εgrow 



≤ K⋆ ≤



ˆρ⋆α

 1 + εprune 



, (28) 

provided this interval is non-empty and has non-empty intersection with {Kmin , . . . , K max }. Otherwise K⋆

equals the boundary of {Kmin , . . . , K max } closest to the infeasible interval. 

Proof sketch The cooldown rule allows at most one event per interval [ tn, t n+1 ), so K(tn) is a sequence of integers in {Kmin , . . . , K max } modulated by at most one ±1 increment per index. The grow condition (19) fires when ˆ ρα(tn) > K (tn)(1 + εgrow ) and is blocked at 7K(tn) = Kmax . The prune condition fires when some head’s relative output πi(tn) < ε prune ; by a balance-of-energy argument (Lemma 15 in Appendix A), this is equivalent on the subsequence to K(tn) ≥ ˆρα(tn)(1 + 

εprune ). Under (27), ˆ ρα(tn) stabilises to ˆ ρ⋆α and neither condition fires for n sufficiently large; the value at which this occurs is K⋆, which lies in the interval (28) by inver-sion of the firing conditions. The detailed bookkeeping, including the effect of the counter thresholds δgrow , δ prune 

on the convergence rate, is given in Appendix A. 2

Remark 12 (Scope of Proposition 11) The exis-tence of the limit (27) is not established in general: 

ˆρα(tn) depends on the distribution of context tokens induced by the current policy, which itself evolves during SAC training, introducing a two-way coupling between 

K(t) and ˆρα(t). Two remarks mitigate this circularity. First, the rank estimator is defined on a rolling buffer of 

N = 10 3 tokens (Section 5.1), which acts as a low-pass filter against transient changes in the policy distribution. Second, in every run of the main campaign of Section 7 the estimator ˆρα(t) was observed to stabilise within the first 2 × 10 4 training steps, well before the nominal train-ing horizon of 5 × 10 4 steps; the numerical trajectories of ˆρα(t) are reported in Figure 4. When (27) fails — for example, in environments where SAC does not con-verge — Proposition 11 does not apply. In that case the runtime controller continues to track ˆρα(t) online, and Proposition 7 and Theorem 9 remain valid event-wise. 

The three results combine to deliver the guarantee re-quested in (P3) of Problem 4. 

Corollary 13 (No-regression guarantee) Consider any training loop that alternates SAC updates with the event rules of Section 5.2, and let {θ(t)}t≥0 denote the resulting meta-controller trajectory. Then: (a) the policy is continuous at every grow event (Propo-sition 7); (b) the cumulative policy jump along the sequence of events is bounded by LMLP εprune Kmax σmax Np(t)

(Theorem 9); (c) the active head count is bounded above by Kmax at every step and, when the empirical effective rank stabilises, converges to a steady-state value within the interval of Proposition 11. Consequently, requirement (P3) of Problem 4 is met with the explicit constants given by the grow/prune thresholds and the architectural hyperparameters of the variable-head attention block. 

Remark 14 (Scope of the guarantees) 

Corollary 13 should not be read as a convergence guar-antee for SAC under the runtime controller. Its role is narrower: it certifies that the controller itself does not introduce policy-trajectory discontinuities beyond what the grow/prune thresholds explicitly allow, and that the active head count does not drift unboundedly. Standard SAC convergence analysis [15] applies unchanged be-tween events, and the cooldown ∆grace provides a tunable separation-of-time-scales between capacity changes and policy updates. 

6 Benchmark and experimental protocol 

The empirical evaluation uses the 2-DOF Stribeck benchmark of [11], with identical plant, reference, pay-load distribution, computed-torque baseline, and evalu-ation metric, to permit a direct numerical comparison between the static-K INCRT-1L of the parent work and the runtime-adaptive policy of the present paper. The plant is a planar two-link manipulator ( n = 2) with gravity-compensated diagonal mass matrix, Stribeck-plus-viscous friction (3), and internal-memory dynam-ics (4). The memory time-constant takes three values 

τz ∈ { 1, 2, 5} s, spanning short, medium, and long mem-ory regimes. Sampling interval is ∆ t = 10 ms and the control horizon T = 500 steps per episode. The reference trajectory is qd(t) = (0 .5 sin t, 0.3 sin 1 .5t); each train-ing episode draws a payload p ∈ [0 , 1.5] kg that modi-fies the effective mass. The computed-torque baseline is 

τ = CT( q, ˙q, q d; K0 

> d

, Λ0) without friction compensation, with K0 

> d

= 30 and Λ 0 = 5, yielding a residual root-mean-square error (RMSE) of approximately 0 .13 rad at every regime. Each policy is evaluated on the fixed grid 

p ∈ {0, 0.375 , 0.75 , 1.125 , 1.5} kg, three deterministic rollouts per payload, for a total of 15 rollouts. The metric is the relative improvement ∆% := RMSE policy − RMSE baseline 

RMSE baseline 

, (29) and a policy is declared successful if its evaluation RMSE is below 0 .10 rad, as in [11, Section 7.3]. The main campaign uses ten seeds per regime ({42 , . . . , 51 }), totalling 30 runs. Each run trains SAC for 5 × 10 4 environment steps with the replay-buffer and update schedule of [11, Appendix B]. A single set of runtime-controller hyperparameters is shared across all three regimes (Table B.1, Ap-pendix B): Kmax = 8, Kinit = 1, εgrow = 0.1, 

εprune = 10 −3, δgrow = δprune = 3, ∆ grace = 2 ,000 steps, 

tchk = 500 steps. Window size W matches the per-regime choice of [11]: W = 20 at τz ∈ { 1, 5} and W = 50 at τz = 2. A Kmax ablation at τz = 2 sweeps Kmax ∈ { 2, 4, 16 }

with five seeds each on a separate compute platform; the 

Kmax = 8 point reuses the main-campaign τz = 2 result 8for statistical power. All experiments were run on Google Colab Pro+ with NVIDIA A100 GPUs; a single 5 × 10 4-step run takes 20–27 min of wall-clock time. Code and seeds are available in the supplementary archive. 

7 Experimental Evaluation 

This section reports the empirical behaviour of Runtime-INCRT on the 2-DOF Stribeck tracking benchmark of [11]. The experiments are designed to answer three questions. (1) Does Runtime-INCRT match or exceed the perfor-mance of the static INCRT-1L baseline across the memory regimes τz ∈ { 1, 2, 5} s? (2) Does Runtime-INCRT resolve the failure mode ob-served in [11] at the long-memory regime τz = 5 s, where INCRT-1L diverged on 4 /10 seeds? (3) What is the role of the maximum head budget 

Kmax : does the runtime controller discover a “nat-ural” rank intrinsic to the task, or does the growth schedule simply saturate at whatever upper bound is prescribed? 

7.1 Experimental setup 

The same 2-DOF Stribeck environment, reward, base-line, and SAC hyper-parameters are adopted as in [11, Appendix B], with the sole architectural change being the replacement of the static INCRT-1L feature extrac-tor by a VarHeadAttention encoder whose active head count is managed at run-time by the controller of Sec-tion 5. The baseline is computed-torque without friction compensation. Each run trains for 5 · 10 4 SAC steps. Evaluation is performed over five payload conditions 

p ∈ { 0, 0.375 , 0.75 , 1.125 , 1.5} kg with three random roll-outs per payload, as in [11]. Success is declared when the evaluation RMSE falls below 0 .10 rad, a ∼ 25% im-provement over the computed-torque baseline. The main campaign uses 10 seeds ( 42 –51 ) per memory regime τz ∈ { 1, 2, 5}, for a total of 30 runs. The runtime configuration is fixed across all runs: Kmax = 8, Kinit =1, εgrow = 0 .1, εprune = 10 −3, δgrow = 3, δprune = 3, ∆grace = 2000 steps, with the controller invoked every 500 steps. The effective-rank estimator retains the last 1000 observations at a 95% energy threshold. 

7.2 Main results 

Table 1 reports the relative improvement ∆% of evalu-ation RMSE with respect to the computed-torque base-line, for INCRT-1L [11] and Runtime-INCRT. Figure 2 visualises the comparison; Figure 3 shows the per-seed distribution of ∆% for Runtime-INCRT.                                              

> Table 1 Relative RMSE improvement ∆% over the computed-torque baseline ( success : eval. RMSE <0.10 rad; intervals are 95% Student-tCIs, n= 10). INCRT-1L (static) Runtime-INCRT
> τz[s] ∆% Fails ∆% ±CI 95 Succ. ∆ (pts) 1−40 .0% 0/10 −45 .82% ±7.09% 10/10 −5.82−35 .0% 0/10 −50 .85% ±8.38% 10/10 −15 .85+5 .0% 4/10 −54 .15% ±5.43% 10/10 −59 .1
> Fig. 2. Mean relative RMSE improvement ∆% (with 95% Student-tconfidence intervals) of Runtime-INCRT (blue,
> n= 10) and static INCRT-1L [11] (grey) over the computed– torque baseline, for each memory regime τz∈ { 1,2,5}s. Er-ror bars for INCRT-1L are not reported in the parent work.
> Fig. 3. Per-seed distribution of ∆% for Runtime-INCRT across all 30 main-campaign runs ( n= 10 per regime). Each dot represents one seed; the horizontal bar marks the mean. Seed variance is smallest at τz= 5 s — the regime where INCRT-1L was most unstable — confirming the stabilising effect of the runtime mechanism.

Short and medium memory ( τz ∈ { 1, 2}). At τz =1 s, Runtime-INCRT achieves ∆% = −45 .82% ± 7.09% (mean ± 95% confidence interval, n = 10), improving by 5 .8 points over the −40 .0% reported for INCRT-1L in [11]. At τz = 2 s the gap widens to 15 .8 points: Runtime-INCRT attains −50 .85% ± 8.38% (10 /10 suc-cess), against −35 .0% for INCRT-1L tuned via Bayesian optimisation with K = 7 and window W = 50. The improvement at the medium-memory regime is notable because INCRT-1L [11] was itself the product of per-9regime hyper-parameter search, yet Runtime-INCRT — starting from a single head ( Kinit = 1) and without any per-regime tuning — outperforms it. 

Long memory ( τz = 5 ): the failure mode is re-solved. This is the regime where INCRT-1L [11] exhibits the pathological behaviour described in Sec-tion 7.3 of [11]: 4 /10 seeds failed to converge, yield-ing a positive mean ∆% = +5 .0% — that is, worse than the computed-torque baseline. Runtime-INCRT eliminates this failure mode: all 10 seeds succeed, with ∆% = −54 .15% ± 5.43% and per-seed values in [−65 .00 , −38 .80]%. The worst seed of Runtime-INCRT at τz = 5 is therefore 44 points better than the mean of [11], and the improvement over [11] at this regime amounts to 59 .1 points. 

Consistency across regimes. Figure 3 shows that the per-seed spread of Runtime-INCRT is not only low in absolute terms (std ≤ 11 .71% across all regimes) but is 

smallest at τz = 5 (std 7 .59%), against 9 .91% at τz = 1 and 11 .71% at τz = 2. INCRT-1L [11], by contrast, con-centrated most of its variance at τz = 5 and produced diverged runs in that regime. This reversal — increased stability exactly where the static architecture was least stable — is the clearest empirical signature of the run-time mechanism. No run across the entire 30-run cam-paign fails to beat the computed-torque baseline; the worst performance observed is ∆% = −31 .16% (seed 47, 

τz = 2). 

Capacity dynamics during training. Figure 4traces the active head count K(t) through training for a representative seed at each τz . A striking feature is the near-invariance of the growth schedule across regimes: in all three cases the controller performs seven growth events — one per cooldown window ∆ grace —and saturates at K = Kmax = 8 around step 1 .9 · 10 4,i.e. within the first 40% of training. Pruning events are not triggered in any of the 30 runs.            

> Fig. 4. Evolution of the active head count K(t) during training for a representative seed at each memory horizon
> τz∈ { 1,2,5}s. Vertical dashed lines mark grow events. In all three cases the controller performs seven growth steps and saturates at K=Kmax = 8 around step 1 .9×10 4, well before the end of training; the growth schedule is effectively task-invariant.

The observation qualifies the interpretation of the method. A naive reading of Runtime-INCRT would be that the controller detects the intrinsic complexity of the task and adapts K accordingly. The capacity dynam-ics observed here are instead largely task-independent under the current hyper-parameter choice: the rank estimator ˆ ρ(t) rises above any plausible K early in the exploration phase of SAC, saturating the growth trigger until Kmax is reached. What drives the improvement over INCRT-1L is therefore not the specific value of the final head count, but the gradual expansion of capacity during training. Each new head is activated with WO = 0 (soft initialisation, Proposition 7), so the encoder starts with a single residual branch, fits the early low-dimensional regime of the tracking task, and gradually inherits additional capacity as training progresses, with each newly-added head starting from a near-optimal residual initialisation. This constitutes an implicit curriculum over capacity, applied within a single training run. The Kmax ablation of Section 7.3 supports this inter-pretation directly: restricting Kmax to 2 (rather than 8) does not degrade performance at τz = 2. 

7.3 Ablation on the head budget Kmax 

To isolate the contribution of the final head count from that of the growth curriculum, an ablation is performed over Kmax ∈ { 2, 4, 8, 16 } at τz = 2. Five seeds are run at Kmax ∈ { 2, 4, 16 } on a separate compute platform; the Kmax = 8 condition is reused from the main cam-paign (Table 1, ten seeds), which affords greater statis-tical power at that value. All other hyperparameters are held fixed at their main-campaign values. The complete ablation is summarised in Table 2 and Figure 5. Three observations are worth isolating. First, the best mean performance is obtained at the 

smallest head-count cap: Kmax = 2 yields ∆% =

−57 .21%, versus −50 .84% at Kmax = 8 and −43 .82% at 

Kmax = 16. All configurations comfortably outperform the Bayesian-optimisation (BO) tuned static INCRT-1L baseline with K = 7 (∆% = −35 .0%, dashed reference in Figure 5). Second, the 95% confidence intervals of the four configurations overlap substantially, so the differ-ences in mean are not, on their own, statistically conclu-sive; what they rule out, however, is the hypothesis that larger Kmax improves performance. On the contrary, the point estimates are consistent with a mild monotone 

degradation as Kmax grows beyond two. Third, per-seed stability is highest at Kmax = 2 ( σ = 6 .96%) and lowest at Kmax = 8 ( σ = 11 .71%); the standard deviation is not monotone in Kmax , but its lowest value is attained at the smallest cap. At Kmax = 16, four of the five seeds reach Kfinal = 16 within the training budget; the fifth ( seed 44 ) saturates at Kfinal = 13, indicating that with this many possi-ble head activations, some runs do not exhaust the cap 10 Table 2 Effect of Kmax at τz = 2 s. The Kmax = 8 row reuses the n =10 main-campaign seeds. Dashed reference: static INCRT-1L with BO-tuned K = 7 (∆% = −35 .0%).                            

> Kmax ∆% ±CI 95 Std n
> 2−57 .21% ±8.64% 6.96% 54−48 .97% ±9.81% 7.90% 58−50 .84% ±8.38% 11 .71% 10 16 −43 .82% ±12 .78% 10 .30% 5Static INCRT-1L ( K= 7) ∆% = −35 .0%

within 5 × 10 4 steps. This provides a first observation of a run in which the growth schedule does not termi-nate at Kmax , consistent with the convergence interval of Proposition 11 when the cap is not the active constraint. Taken together, these observations support the reading of the runtime mechanism as a curriculum over capacity, rather than as an adaptation of final capacity: increasing the representational budget beyond Kmax = 2 does not help, and in the Kmax = 16 case it slightly hurts. The benefit of Runtime-INCRT comes from the schedule, not the ceiling.                 

> Fig. 5. Effect of the head-count cap Kmax on Runtime-IN-CRT performance at τz= 2 s (25 runs total; n= 5 except
> Kmax = 8 where n= 10). Left: mean ∆% with 95% CI; black dots show individual seeds. The dashed red line is the static INCRT-1L reference ( K= 7, BO-tuned, ∆% = −35 .0%).
> Right: per-seed standard deviation. Smaller Kmax yields bet-ter mean performance and lower variance.

7.4 Sensitivity to the cooldown ∆grace 

The Kmax ablation above suggests that the performance of Runtime-INCRT is governed by the growth schedule rather than by the terminal capacity. The schedule, in turn, is paced by the cooldown ∆ grace : the minimum sep-aration between consecutive grow events (Section 5.2). If the curriculum reading is correct, varying ∆ grace should have a substantial and interpretable effect — unlike the other runtime hyperparameters, which a preliminary null-sensitivity analysis finds not to bind on this bench-mark. The sweep spans ∆ grace ∈ { 1 000 , 2 000 , 5 000 }

with five seeds per value at τz = 2, all other hyperpa-rameters held at their main-campaign values. Results are reported in Table 3 and Figure 6.                                

> Table 3 Sensitivity to ∆ grace at τz= 2 s (5 seeds per value). Failure :eval. RMSE >0.10 rad. Schedule length in units of 10 3steps. ∆grace ∆% ±CI 95 Std Fails Sched. [k] 1 000 −41 .16% ±24 .45% 19 .69% 1/513 .52 000 −51 .66% ±15 .01% 12 .09% 0/519 .55 000 −36 .61% ±38 .31% 30 .85% 1/537 .5

The result is a pronounced non-monotone sensitivity, with a clear optimum at the default value. At ∆ grace =1 000 (fast curriculum), the mean performance drops to ∆% = −41 .16% and seed 46 produces a near-failure at ∆% = −6.63%; the per-seed standard deviation is 19 .69%. At ∆ grace = 2 000 (default), the mean is 

−51 .66% and no seed fails. At ∆ grace = 5 000 (slow cur-riculum), the mean degrades further to −36 .61%, one seed fails outright (∆% = +15 .96%, worse than the base-line), and the per-seed standard deviation is 30 .85%. The confidence intervals of the three conditions overlap substantially, but both the mean shift and the variance inflation at the extremes are consistent and point in the same direction: the curriculum has a characteristic time-scale, and departing from it in either direction erodes the policy’s terminal quality. 

Interpretation. The growth schedule reaches K =

Kmax after seven cooldown-separated events, so the schedule length is approximately 7∆ grace + t0, where 

t0 = 1 500 steps is the training step at which the first grow fires. At ∆ grace = 1 000 the schedule completes within the first ∼ 14 × 10 3 steps of training, leaving 36 ×10 3 steps of SAC updates under the full architecture but compressing the curriculum into a period where the critic has not yet stabilised; at ∆ grace = 5 000 the sched-ule extends to ∼ 38 × 10 3 steps, leaving only 12 × 10 3

post-growth steps for the full-capacity policy to stabilise, and exposing the training to a failure mode when the fi-nal critic has not yet adapted to the last growth event. The default ∆ grace = 2 000 balances these two concerns: the curriculum completes at ∼ 20 × 10 3 steps, leaving 30 ×10 3 post-growth steps — enough for the full-capacity critic to stabilise, but not so long that the early-training dynamics are dominated by a single capacity level. The observation confirms, quantitatively, that the curricu-lum pace matters in the way predicted by the interpre-tation of Section 8.2: neither too fast nor too slow. 

8 Discussion and conclusions 

8.1 The runtime mechanism is not an adaptive sizing of K

One might reasonably hypothesise that the failure of fixed-K INCRT-1L at τz = 5 reflects a capacity deficit, so that a runtime mechanism capable of adapting K to 11 Fig. 6. Sensitivity of Runtime-INCRT to the cooldown pa-rameter ∆ grace at τz = 2 s (5 seeds per value). Left: mean ∆% with 95% CI. Right: per-seed standard deviation. Per-formance is non-monotone: both the fast (1 000 steps) and the slow (5 000 steps) schedule degrade the mean and inflate variance relative to the default (2 000 steps), confirming a characteristic curriculum time-scale. 

task complexity would resolve the failure by allocating additional heads at long memory. The experiments of Section 7 contradict this reading. Under the default hy-perparameter setting, the active head count K(t) reaches 

Kmax in every run of the main campaign and in every run of the Kmax ablation at Kmax ∈ { 2, 4, 8}; the growth schedule is task-invariant, the prune trigger never fires, and at Kmax = 16 one of five ablation seeds fails to ex-haust the cap within the training budget. More tellingly, the smallest cap Kmax = 2 outperforms the BO-tuned static baseline with K = 7 by approximately 22 points and outperforms the Kmax = 16 runtime variant by 13 points. A smaller network cannot represent functions that a larger network cannot; if capacity were the bot-tleneck, the Kmax = 2 runtime policy would underper-form the K = 7 static policy, and it does not. The mech-anism driving the improvement therefore acts through the training trajectory, not through the terminal repre-sentational capacity. 

8.2 A curriculum interpretation 

The above observations point to a specific mechanism. Each growth event is non-disruptive (Proposition 7): the policy output is unchanged, the SAC replay buffer re-mains on-policy, and the optimiser resumes from its pre-event state. The encoder therefore starts as a single-head attention block, SAC explores a correspondingly simpler effective state space in which the local-attractor struc-ture of the full-capacity problem has not yet appeared, and additional heads are activated gradually, each ini-tialised with zero residual contribution and trained against the existing policy. The growth schedule plays the role of a capacity curriculum: the optimisation be-gins on a tractable subproblem and is escorted through a sequence of increasingly rich representations, with each transition guaranteed policy-preserving. This reading is consistent with the reversal of the stabil-ity ranking across τz . Under the static baseline, τz = 5 is the hostile regime (variance largest, failure rate 4 /10); under Runtime-INCRT, τz = 5 is the most stable regime (per-seed standard deviation 5 .43%, zero failures). The long-memory regime is the regime in which the full-capacity attractor landscape is most hostile, and there-fore the regime that benefits most from the gradual traversal of capacity levels. It is also consistent with the observation that per-seed stability is highest at 

Kmax = 2: a policy forced to operate in a larger param-eter space from the outset is more likely to be captured by a local attractor; a policy allowed to warm up in a small-capacity subspace is less exposed. The quantita-tive characterisation of this effect is beyond the scope of the present paper. The curriculum reading is not only consistent with the post-hoc observations above; it also makes a concrete fal-sifiable prediction that the present paper can test. If the mechanism is the schedule — rather than, say, some arte-fact of the Xavier initialisation or the optimiser rebuild — then the pace of the schedule, set by the cooldown ∆grace , should have a pronounced effect on the policy quality, and specifically should exhibit a characteristic time-scale: too fast, and each new head is injected be-fore the critic has stabilised its evaluation of the previ-ous capacity level; too slow, and the terminal capacity is reached too close to the end of the training budget for SAC to consolidate. The sensitivity sweep of Section 7.4 confirms precisely this shape: both ∆ grace = 1 000 (fast) and ∆ grace = 5 000 (slow) substantially degrade perfor-mance and inflate seed variance relative to the default ∆grace = 2 000, with one catastrophic failure per ex-treme. A mechanism driven by terminal capacity alone would not predict this non-monotone pace-dependence; the curriculum mechanism does. The curriculum view complements the literature on task-difficulty curricula [3,14] and the progressive-growing regime of generative adversarial networks [19]. What is distinctive here is that the curriculum is internal to a single RL training run, endogenously driven by the rank of the on-policy context-token distribution, and oper-ates without external task schedules. The architectural guarantees of Section 5 — policy continuity at growth, bounded jump at pruning, convergence of the active head count — are what makes such an internal capacity curriculum tractable, by ruling out the restart-and-resize pathology that would otherwise invalidate the SAC re-play buffer. 

8.3 Limitations and future work 

The guarantees of Section 5 are architectural: they cer-tify that the runtime mechanism does not introduce pol-icy discontinuities beyond the prune threshold, but do not provide a convergence guarantee for SAC under the combined training loop. Standard SAC convergence re-sults apply between events, and the cooldown ∆ grace 

provides a tunable separation-of-time-scales, but a full analysis of the closed loop remains open. The prune trigger has not been activated in any experiment re-ported here; the bound of Theorem 9 is the architectural 12 machinery that keeps the corresponding policy jumps bounded when the trigger does fire. Scaling the runtime mechanism to higher-degree-of-freedom Euler–Lagrange systems is the natural next step, as is the integration with the Lyapunov shielding of [10]. A further direction is the theoretical question of whether the “zero-loss-at-growth plus policy continuity” pattern can be gener-alised beyond attention heads — for instance, to MLP-width growth, to residual-branch growth, or to modality growth in multi-input architectures. 

8.4 Conclusions 

The evidence gathered on the two-link Stribeck bench-mark invites a reinterpretation of what runtime capacity adaptation actually buys, at least in the reinforcement-learning regime studied here. The quantity that matters is not the terminal number of heads, which saturates at the cap regardless of task, but the training trajectory through intermediate capacities. Gradual growth acts as a capacity curriculum internal to a single run, replacing the offline Bayesian search over a scalar architectural hy-perparameter with an endogenous process whose pace is set by a single time-scale separation parameter ∆ grace .Two implications follow. First, the offline-search pipeline used by fixed-capacity meta-controllers trades experi-menter time for a brittle quantity: the transfer of the tuned head count to a slightly different operating regime is not guaranteed and, at long memory horizons, has been observed here to collapse. A runtime mechanism that does not rely on such transfer is more robust. Sec-ond, the architectural guarantees of Section 5 are what make the mechanism tractable: without policy conti-nuity at growth, without a bounded jump at pruning, and without convergence of the active head count, the gradual-growth idea would degenerate into a restart-and-resize loop incompatible with the on-policy nature of the reinforcement-learning objective. A less parochial reading of these results is that the in-terplay between online optimisation and architectural dynamics deserves systematic study beyond the spe-cific setting of multi-head attention. Whether analogous capacity-curriculum effects arise for MLP-width growth, residual-branch growth, or modality growth in multi-input architectures, and whether the required architec-tural continuity can be preserved across such generali-sations, are open questions that the present work moti-vates but does not settle. 

A Proofs 

This appendix provides the technical details deferred from Section 5. A balance-of-energy lemma relating the prune-triggering condition to the empirical effective rank is established in Section A.1; the full proof of Proposi-tion 11 follows in Section A.2. 

A.1 A balance-of-energy lemma 

Throughout this appendix, fix the check cadence tchk 

and consider the cooldown-separated subsequence tn =

n ∆grace at which grow and prune events may occur. Let Bt denote the context-token buffer of Definition 5, 

ht the encoder output on the fresh mini-batch used by the prune trigger (see Section 5.2), and ηj (t) the per-head output norms. Denote the total active-head output magnitude 

S(t) := X   

> j:mj(t)=1

ηj (t), (A.1) so that πj (t) = ηj (t)/S (t) is the relative contribution of head j.

Lemma 15 (Balance of rank and head count) 

Suppose ˆρα(tn) stabilises at ˆρ⋆α, and that the per-head output norms are approximately equidistributed on the subsequence, in the sense that there exists a constant 

β ≥ 1 such that 

max     

> j:mj(tn)=1

πj (tn) ≤ β min     

> j:mj(tn)=1

πj (tn)

∀ n sufficiently large . (A.2) 

Then the prune trigger fires on the subsequence if and only if 

K(tn) > ˆρ⋆α

 1 + εprune 

 + β − 1, (A.3) 

with the β − 1 slack vanishing in the equidistributed limit 

β = 1 .

Proof. For fixed n, let Kn := K(tn). By (A.2), min j πj (tn) ≥ (1 /β ) (1 /K n), since the sum of relative contributions is 1 and the maximum is at most β times the minimum. The prune trigger fires on head i if and only if πi(tn) < ε prune ; substituting the lower bound gives 1

βK n

≤ πi(tn) < ε prune ,

which yields Kn > 1/(β ε prune ). Combined with the rank-saturation condition ˆ ρα(tn) ≤ Kn (necessary for the grow trigger to be inactive) and the stabilisation ˆρα(tn) = ˆ ρ⋆α, one obtains the threshold (A.3). Equidistri-bution β = 1 recovers the form used in the proof sketch of Proposition 11. 2

Remark 16 (Verification of (A.2) ) The equidistri-bution hypothesis is observed empirically in the experi-ments of Section 7: at convergence of K(t), the per-head output norms are within a factor β ≈ 2–3 of each other. In regimes where a single head dominates (e.g., degener-ate initialisations), β grows large and the prune trigger becomes conservative; this is the behaviour one would expect from a safety-oriented pruning rule. 

13 A.2 Proof of Proposition 11 

Proof of Proposition 11 Let tn = n ∆grace be the cooldown-separated subsequence, and Kn := K(tn) the active head count. Under (27), there exists N0 ∈ N such that ˆ ρα(tn) = ˆ ρ⋆α for all n ≥ N0.

Step 1: Eventual monotonicity. The event rules of Section 5.2 allow at most one event per interval [tn, t n+1 ), so Kn+1 − Kn ∈ {− 1, 0, +1 }. The rules fur-ther require Kn ∈ { Kmin , . . . , K max } at all times (equa-tions (19) and the prune condition are both gated on boundary checks). 

Step 2: Both triggers inactive imply Kn constant. 

Inspecting the firing conditions: 

• Grow fires iff ˆ ρ⋆α > K n(1 + εgrow ) for δgrow consec-utive checks, equivalently Kn < ˆρ⋆α/(1 + εgrow ). 

• Prune fires iff some πj (tn) < εprune for δprune 

consecutive checks. By Lemma 15 applied in the equidistributed limit, this is equivalent to Kn >

ˆρ⋆α(1 + εprune ). Both triggers are therefore inactive — and Kn constant — on the interval 

I⋆ := 

 ˆρ⋆α

1 + εgrow 

, ˆρ⋆α(1 + εprune )



. (A.4) 

Step 3: Reachability of I⋆ ∩ { Kmin , . . . , K max }. The claim is that, for all n sufficiently large, Kn ∈ I ⋆ ∩{Kmin , . . . , K max } (the inactive interval , assumed non-empty). Consider the dynamics of Kn outside I⋆:

• If Kn < inf I⋆, the grow trigger is active. After at most δgrow consecutive cooldown-separated checks, 

Kn+δgrow = Kn + 1 (unless Kn = Kmax , in which case the grow is blocked). 

• Symmetrically, if Kn > sup I⋆, the prune trigger is active and Kn+δprune = Kn −1 (unless Kn = Kmin ). Since Kn is an integer and |Kn+1 − Kn| ≤ 1, the sequence Kn enters I⋆ in at most ( Kmax − Kmin ) ·

max( δgrow , δ prune ) cooldown intervals. If I⋆ does not in-tersect {Kmin , . . . , K max }, the sequence Kn clamps at the boundary of this set closest to I⋆ (either Kmin or 

Kmax ) and remains there, which is the second clause of the statement. 

Step 4: Absorption in I⋆. Once Kn ∈ I ⋆, by Step 2 neither trigger fires and Kn+1 = Kn. By induction, 

Km = Kn for all m ≥ n, establishing convergence to 

K⋆ := Kn. The bound (28) follows by taking K⋆ to be the unique integer in I⋆ ∩ { Kmin , . . . , K max } visited by the sequence (which is well-defined because I⋆ is an in-terval of width ˆ ρ⋆α(εgrow + εprune )/(1 + εgrow ), which is bounded but in general contains one or more integers for the parameter values of Section 5.2). 

Step 5: Convergence rate. The number of cooldown intervals required for Kn to reach the inactive interval is bounded by 

Nconv ≤ max  δgrow , δ prune 

 · Kinit − K⋆ ,

corresponding to a total training-step bound of Nconv ·

∆grace . For the parameter values of Section 5.2 ( δgrow =

δprune = 3, ∆ grace = 2 × 10 3, Kmax = 8, Kinit = 1), the convergence time is at most 3 · 7 · 2 × 10 3 = 4 .2 ×

10 4 steps, consistent with the empirical observation that 

K(t) saturates within 2 × 10 4 steps (Section 7). 2

Remark 17 (Non-equidistributed case) If the equidistribution constant β of Lemma 15 is strictly greater than 1, the upper endpoint of I⋆ in (A.4) is replaced by ˆρ⋆α(1 + εprune ) + β − 1. The interval widens by β − 1 and the convergence argument remains valid with the modified interval. In Section 7, β is observed in the range [2 , 3] , and the corresponding upper endpoint is never tight in practice because the prune trigger never fires on the experimental benchmarks. 

B Hyperparameters and implementation notes 

B.1 Runtime controller and architecture B.2 Training (SAC) 

The Soft Actor-Critic [15] configuration matches that of [11, Appendix B]: 

• optimiser: Adam with lr = 3 × 10 −4;

• discount γ = 0 .99; 

• target smoothing τ = 0 .005; 

• replay buffer size 5 × 10 4;

• batch size 256; 

• entropy coefficient: automatic (SB3 default); 

• MLP head width [64 , 64]; 

• total training budget 5 × 10 4 environment steps per run. 

B.3 Implementation notes 

• The variable-head attention block is im-plemented as a stable baselines3. BaseFeaturesExtractor subclass with per-head 

nn.Parameter fields and a boolean active mask. 14 Table B.1 Runtime-INCRT hyperparameters (same values across all 

τz ∈ { 1, 2, 5} s). Parameter Value Role 

Architecture 

Kmax 8 head-count cap 

Kinit 1 initial active heads 

Kmin 1 minimum active heads 

dk 16 per-head key/value dim. 

dmodel 128 = Kmax · dk

Runtime controller 

εgrow 0.10 grow trigger threshold 

εprune 10 −3 prune trigger threshold 

δgrow 3 confirmations to grow 

δprune 3 confirmations to prune ∆grace 2 000 cooldown (steps) 

tchk 500 check cadence (steps) 

Rank estimator 

N 1 000 context-token buffer size 

α 0.95 energy threshold 

Window size Wτz ∈ { 1, 5} s 20 

τz = 2 s 50 

• At every grow or prune event, the SAC optimisers for the actor and critic networks are rebuilt from the current set of trainable parameters, carrying the current learning rate forward from the scheduler. 

• Numerical verification of Proposition 7 is per-formed at every grow event by evaluating the at-tention block on a fresh batch before and after the mask update. The observed L∞ difference is 0 to 32-bit floating-point precision across all 30 main-campaign runs. 

• All experiments were performed on Google Colab Pro+ with NVIDIA A100 GPU. Wall-clock time for a single 50k-step run is approximately 20–27 minutes. 

References 

[1] Marcin Andrychowicz, Filip Wolski, Alex Ray, Jonas Schneider, Rachel Fong, Peter Welinder, Bob McGrew, Josh Tobin, Pieter Abbeel, and Wojciech Zaremba. Hindsight experience replay. In Advances in Neural Information Processing Systems 30 (NeurIPS) , pages 5048–5058, 2017. [2] Brian Armstrong-H´ elouvry, Pierre Dupont, and Carlos Canudas de Wit. A survey of models, analysis tools and compensation methods for the control of machines with friction. Automatica , 30(7):1083–1138, 1994. [3] Yoshua Bengio, J´ erˆ ome Louradour, Ronan Collobert, and Jason Weston. Curriculum learning. In Proc. 26th International Conference on Machine Learning (ICML) ,pages 41–48, 2009. [4] C. Canudas de Wit, H. Olsson, K. J. ˚Astr¨ om, and P. Lischinsky. A new model for control of systems with friction. IEEE Transactions on Automatic Control ,40(3):419–425, 1995. [5] Cheng Chen, Yichun Yin, Lifeng Shang, Xin Jiang, Yujia Qin, Fengyu Wang, Zhi Wang, Xiao Chen, Zhiyuan Liu, and Qun Liu. bert2BERT: Towards reusable pretrained language models. In Proc. 60th Annual Meeting of the Association for Computational Linguistics (ACL) , pages 2134–2148, 2022. [6] Lili Chen, Kevin Lu, Aravind Rajeswaran, Kimin Lee, Aditya Grover, Michael Laskin, Pieter Abbeel, Aravind Srinivas, and Igor Mordatch. Decision transformer: Reinforcement learning via sequence modeling. In Advances in Neural Information Processing Systems 34 (NeurIPS) , pages 15084–15097, 2021. [7] Tianqi Chen, Ian J. Goodfellow, and Jonathon Shlens. Net2net: Accelerating learning via knowledge transfer. In International Conference on Learning Representations (ICLR) , 2016. [8] Kyunghyun Cho, Bart van Merri¨ enboer, Caglar Gulcehre, Dzmitry Bahdanau, Fethi Bougares, Holger Schwenk, and Yoshua Bengio. Learning phrase representations using RNN encoder–decoder for statistical machine translation. In Proc. 2014 Conference on Empirical Methods in Natural Language Processing (EMNLP) , pages 1724–1734, 2014. [9] Giansalvo Cirrincione. INCRT: An incremental transformer that determines its own architecture. arXiv preprint arXiv:2604.10703, 2026. [10] Giansalvo Cirrincione and Adriano Fagiolini. Learned Lyapunov certificates for safe residual reinforcement learning on Euler–Lagrange systems. Manuscript submitted to the International Journal of Robust and Nonlinear Control, 2026. [11] Giansalvo Cirrincione and Adriano Fagiolini. Temporal attention for adaptive control of Euler–Lagrange systems with unobservable memory. Manuscript submitted to the International Journal of Robust and Nonlinear Control, 2026. [12] Scott E. Fahlman and Christian Lebiere. The cascade-correlation learning architecture. In Advances in Neural Information Processing Systems 2 (NeurIPS) , pages 524–532, 1990. [13] Linyuan Gong, Di He, Zhuohan Li, Tao Qin, Liwei Wang, and Tie-Yan Liu. Efficient training of BERT by progressively stacking. In Proc. 36th International Conference on Machine Learning (ICML) , volume 97 of PMLR , pages 2337–2346, 2019. [14] Alex Graves, Marc G. Bellemare, Jacob Menick, R´ emi Munos, and Koray Kavukcuoglu. Automated curriculum learning for neural networks. In Proc. 34th International Conference on Machine Learning (ICML) , volume 70 of PMLR , pages 1311–1320, 2017. [15] Tuomas Haarnoja, Aurick Zhou, Pieter Abbeel, and Sergey Levine. Soft actor-critic: Off-policy maximum entropy deep reinforcement learning with a stochastic actor. In Proceedings 

15 of the 35th International Conference on Machine Learning (ICML) , pages 1861–1870, 2018. [16] Song Han, Jeff Pool, John Tran, and William J. Dally. Learning both weights and connections for efficient neural networks. In Advances in Neural Information Processing Systems 28 (NeurIPS) , pages 1135–1143, 2015. [17] Sepp Hochreiter and J¨ urgen Schmidhuber. Long short-term memory. Neural Computation , 9(8):1735–1780, 1997. [18] Michael Janner, Qiyang Li, and Sergey Levine. Offline reinforcement learning as one big sequence modeling problem. In Advances in Neural Information Processing Systems 34 (NeurIPS) , pages 1273–1286, 2021. [19] Tero Karras, Timo Aila, Samuli Laine, and Jaakko Lehtinen. Progressive growing of GANs for improved quality, stability, and variation. In International Conference on Learning Representations (ICLR) , 2018. [20] Hanxiao Liu, Karen Simonyan, and Yiming Yang. DARTS: Differentiable architecture search. In 7th International Conference on Learning Representations (ICLR) , 2019. [21] Pavlo Molchanov, Stephen Tyree, Tero Karras, Timo Aila, and Jan Kautz. Pruning convolutional neural networks for resource efficient inference. In International Conference on Learning Representations (ICLR) , 2017. [22] Hieu Pham, Melody Y. Guan, Barret Zoph, Quoc V. Le, and Jeff Dean. Efficient neural architecture search via parameter sharing. In Proceedings of the 35th International Conference on Machine Learning (ICML) , pages 4095–4104, 2018. [23] Jean-Jacques E. Slotine and Weiping Li. On the adaptive control of robot manipulators. International Journal of Robotics Research , 6(3):49–59, 1987. [24] Mark W. Spong, Seth Hutchinson, and M. Vidyasagar. Robot Modeling and Control . Wiley, Hoboken, NJ, 2nd edition, 2020. 

16